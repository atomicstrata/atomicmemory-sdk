/**
 * @file KV cache Type Definitions (placeholder)
 *
 * Type definitions for KV / cache storage interfaces. These types are
 * available directly from '../kv-cache/storage-adapter'. Artifact-storage
 * integrations should use the public `./storage` subpath.
 */

export {};
