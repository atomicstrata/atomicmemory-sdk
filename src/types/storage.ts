/**
 * @file KV cache Type Definitions (placeholder)
 *
 * Type definitions for KV / cache storage interfaces. These types are
 * available directly from '../kv-cache/storage-adapter'. The directory was
 * renamed from `./storage` to `./kv-cache` in the storage-sibling rev so the
 * new storage API can own `./storage` — see plan rev-6 finding 1.
 */

export {};
